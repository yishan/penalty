<p align="right">
  <strong>简体中文</strong> · <a href="validation.md">English</a>
</p>

# Penalty — 验证记录与真机清单

更新：2026-09-18。本文区分代码实现与实物验收。

## v1.1 视觉更新——当前本地固件

竖屏封面在 `PENALTY` 下新增固定中文游戏名“点球决胜”；所有当前球场素材统一改为横向草坪明暗条纹；门将改穿黑色球衣。横屏标题页不再绘制原有英文／中文口号。规则、操作、概率校准、多语言行为、声音与持久保存均保持 v1.0 不变。

- ESP-IDF 5.5.3 完整固件构建与合并镜像验证：PASS。应用大小 1,222,656 字节，未改变的 factory 分区仍剩余 85%。
- 静态／宿主门禁：PASS。仓库检查、模型、场景、多语言、偏好保存、动态文案、字体、美术合约、导航、显示及固件布局测试全部通过。
- 确定性美术合约：PASS，覆盖 240 x 320 竖屏封面、320 x 240 游戏背景、1086 x 1448 社区封面候选图、封面中文标识精确合成、横向条纹方向，以及当前运行时门将素材中蓝色球衣像素归零。
- LVGL 9.5.0 实际渲染：PASS，覆盖竖屏封面、中英文标题、游戏画面，以及 24 KB 内存池内连续 50 局／50 次进入退出。预热后剩余内存 12,248 字节，最大运动帧重绘 27,535 像素。检查合成图位于 `build/penalty-art-v1.1-preview/review.png`；这是桌面 LVGL 渲染，不是实机照片。
- 真机验证：PASS。下方 v1.1 合并镜像已刷入真机并实际运行，颜色／对比度、封面中文可读性、横向条纹可辨性以及时机／声音表现均不再属于未验证项。

```text
File: build/Penalty-art-v1.1-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1288192 bytes
Flash offset: 0x0
SHA-256: 98aeea498634309fe4d10248347c513dd7b212e3d9ce02d651a7400c5ba66607
Build: PASS
Host tests: PASS
Device tests: PASS
Unverified: 本版本未记录实测的堆／栈、帧率节奏与耐久数据
```

本次视觉更新即使用这个带版本名的文件完成真机验收。v1.0 文件作为历史固件保持不变。v1.1 社区封面候选图尚未上传，现有社区审核状态未改变。

## v1.0 六格射门规则——历史本地固件

球门现分为上下两行六格。LEFT／RIGHT 沿闭合环顺／逆时针切换，门将独立预判同样的六格之一。门将等概率预判后，两格深绿线、其余绿区和正常力度绿区外的最终平均进球率分别标定为 90%、65% 和 25%。弱射、过高和超时仍判失败。深绿线被扑仍计入精准次数，但不计进球。

- 完整固件构建及合并镜像校验：PASS，使用 ESP-IDF 5.5.3；应用为 1,222,672 字节，不变的 factory 分区仍有 85% 空余。
- 静态／宿主门禁：PASS。模型穷举覆盖 363,600 种玩家目标、门将目标、力度和 0–99 随机值组合；六个玩家目标分别都能得到精确的 90%／65%／25% 等概率平均值。
- 实际 LVGL 9.5.0 渲染：六个选中目标、全部目标／结果轨迹、三类扑救的手套接触、中英文界面，以及 24 KB 池内连续 50 局／50 次进入退出全部通过。预热后退出可用内存仍为 12,248 字节，已测运动帧最大重绘 27,535 像素。
- 六个目标选择截帧合成在 `build/penalty-six-target-preview/six-target-grid.png`。它们是桌面 LVGL 实际渲染，不是实机照片。本次未刷写板卡。

```text
File: build/Penalty-six-target-v1.0-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1288208 bytes
Flash offset: 0x0
SHA-256: c2f2ae3cb2b09c60fc0554bd5b9f32d167c1d9e35ff065f154f7f2f8609fd901
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 实物六格可读性与操作手感、统计抽样、声音／触球同步、
  按键／显示刷新、板卡堆／栈及耐久
```

六格规则真机验收请使用该带版本号文件。v0.9 文件作为历史产物保持不变。

## v0.9 中英文界面——历史本地固件

英文与简体中文现已覆盖竖屏进入提示、标题、设置、说明、游玩、结果、总结及低内存降级页。标题包含“开始、设置、说明”，设置页包含“难度、音效、语言、返回”。切换语言会立即刷新当前页面，并在 LVGL／输入路径之外排队写入 NVS；NVS 不存在、值无效或不可用时安全回退英文。

- 完整固件构建及合并镜像校验：PASS，使用 ESP-IDF 5.5.3；应用为 1,219,696 字节，不变的 factory 分区仍有 85% 空余。
- 静态／宿主门禁：PASS。模型、本地化、偏好编码与回退、异步保存生命周期、音频映射、导航、字形契约、渲染及既有玩法测试全部通过。
- 14 px 与 20 px 思源黑体生成子集覆盖当前全部 164 个本地化字符，遵循 SIL OFL 1.1；固件未链接完整 CJK 字库或运行时加载器。
- 实际 LVGL 9.5.0 渲染：中文标题、设置、说明、瞄准、总结、竖屏提示，以及英文覆盖和 24 KB 池内连续 50 局／50 次进入退出均通过。真实字库文字宽度检查通过，预热后退出可用内存仍为 12,248 字节；已测运动帧最大重绘 27,535 像素。
- 本次未刷写板卡。中文实物可读性、真实重启后的 NVS 保留、实际声音和同步仍需真机检查。将此合并镜像从 `0x0` 刷入会重置 NVS 区域；需要保留已有偏好时应采用文档中的分段更新。

```text
File: build/Penalty-bilingual-v0.9-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1285232 bytes
Flash offset: 0x0
SHA-256: 6eff89689f5d8a224726a0a1ee40894d336a5eacaa22417efe92d035b861e313
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 中文实物可读性、真实重启后的语言持久保存、声音／触球同步、
  按键／显示刷新、板卡堆／栈及耐久
```

中英文界面与混合音效真机验收请使用该带版本号文件。选择中文后，退出再进入游戏及正常重启应保持中文；重新刷入合并镜像属于新的 NVS 状态，会恢复英文。

## v0.8 动态文案——历史本地固件

底部完美线数值已改为按状态选择的文案。EASY 每球保留 `DARK LINE = PERFECT`；NORMAL／HARD 仅第一球显示，之后每球固定选择一句鼓励，相邻不重复，其第五球有专属文案。各种结果分别显示对应轻松解说。文案选择不推进玩法随机数，也不改变任何射门判定。

- 完整固件构建及合并镜像校验：PASS，使用 ESP-IDF 5.5.3；应用为 1,133,200 字节，不变的 factory 分区仍有 86% 空余。
- 静态／宿主门禁：PASS。纯 C 测试覆盖教学边界、选择稳定、相邻不重复、最后一球、全部结果映射及 28 字符上限。
- 实际 LVGL 9.5.0 渲染：普通互动句、最后一球、全部结果句及 24 KB 池内连续 50 局／50 次进入退出均通过；文字宽度检查通过，预热后退出可用内存仍为 12,248 字节。
- v0.7 混合音效、视觉、射门规则和社区审核状态均未改变。本次未刷写板卡。

```text
File: build/Penalty-dynamic-copy-v0.8-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1198736 bytes
Flash offset: 0x0
SHA-256: c05a090460e578368e3b4c3cf74a2ecb94428b3cd92cae0b6d479421b7dcb83c
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 实物文案可读性／语气、实际声音与同步、按键／显示刷新、
  板卡堆／栈及耐久
```

动态文案与混合音效真机验收请使用该带版本号文件。

## v0.7 混合音效——历史本地固件

已修正静音的软件根因：16 kHz、16 位单声道格式打开后，还必须成功设置明确的 60% 音量。界面、难度、静音、失误、奖励和五球结束反馈使用短合成音；踢球、手套扑救及克制的进球冲击使用从 Flash 分块播放的原创固定算法 PCM。踢球声在画面 100 ms 触球边界发出，不再在提交射门时提前响；超时只播放失误提示，不会伪造踢球声。

- 完整固件构建及合并镜像校验：PASS，使用 ESP-IDF 5.5.3；应用为 1,132,576 字节，不变的 8 MB 分区布局中 factory 分区仍有 86% 空余。
- 静态／宿主门禁：PASS。新增纯 C 音频测试覆盖触球时机、超时不起脚、全部结果类型、难度／静音／全场结束映射、提示优先级及资源重建；既有模型、场景、导航、生命周期、显示和固件布局检查仍通过。
- 音频生命周期：格式和音量失败分别记录；只有两者都成功，界面才显示声音可用。四项有界语义队列丢弃过期提示，允许动作／结果替换尚未播放的界面音，并在静音／退出时按 10 ms 分片协作取消。
- PCM 占用共 9,280 字节，格式为 16 kHz、有符号 16 位单声道；踢球 80 ms、扑救 100 ms、进球 110 ms。固定算法源及 CC0-1.0 条款已记录在资源说明。
- 本次未刷写板卡。编译通过和 PCM 写入成功不能证明实物扬声器可听、无杂音或同步正确。

```text
File: build/Penalty-hybrid-audio-v0.7-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1198112 bytes
Flash offset: 0x0
SHA-256: 4b8a2726a548bf4c8b03688c0bad091ab9bddffa970ef36ede80203a478f8563
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 实际发声、40/60/80% 校准、音色／削波／杂音、触球同步、
  快速退出重进、板卡堆／栈及耐久
```

混合音效真机测试请使用该带版本号文件。当前默认 60% 是刻意保留的暂定值；只有第一次实机运行过响、过轻或失真时，再比较专门的 40%／80% 校准构建。封面、游戏画面、规则和社区审核状态均未改变。

## v0.6 封面小调整——历史本地固件

竖屏开机封面现已隐藏实时电量文字，横屏标题、玩法、瞄准、飞行、结果与总结页仍保留电量。游戏美术、玩法机制、按键映射和射门时序均与 v0.5 一致。独立的无底部标识社区海报记录在[社区提交记录](community-submission.zh_CN.md)，本次未上传。

- 完整 `./tools/validate.sh`：PASS，使用 ESP-IDF 5.5.3；应用为 1,120,784 字节，合并镜像和不变的分区布局均通过校验。
- 实际 LVGL 9.5.0 渲染：24 KB 池内连续 50 局／50 次进入退出通过，预热后退出可用内存为 12,248 字节。竖屏封面现在对整张内嵌资源逐像素检查，包括原电量区域；横屏文字宽度和游戏渲染检查仍通过。
- 仓库静态、模型、场景、导航、独立生命周期、固件布局与资源重建检查：PASS。未刷写设备。

```text
File: build/Penalty-cover-clean-v0.6-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1186320 bytes
Flash offset: 0x0
SHA-256: b1031c4da997cce3a50a2f763cfbdff5ae93afff537b9288afaff45c6ae01b17
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 实物封面／游戏可读性、刷新、按键响应、声音／触球时机、
  横屏电量、板卡堆／栈／耐久及宿主 ASan
```

本次封面调整请使用该带版本号文件。v0.5、v0.4 与 v0.3 固件副本均保持不变。

## v0.5 像素美术——历史本地固件

已接入确认的球场、三张射手姿势、四张门将姿势与六档足球尺寸。v0.4 玩法规则、竖屏封面、按键映射和 600 ms 射门表现总时长不变，本次未上传社区或刷写设备。

- 完整 `./tools/validate.sh`：PASS，使用 ESP-IDF 5.5.3；应用为 1,120,736 字节，合并镜像和不变的分区布局均通过校验。记录：`build/penalty-art-validation.log`。
- 原规则测试及新增纯动画测试：PASS，动画也通过 UndefinedBehaviorSanitizer。覆盖支撑脚／触球锚点、全部有效射门方向／门将／结果组合、前摇与到达边界、扑出后停球、超时不起脚及踢高范围。
- 实际 LVGL 渲染：24 KB 池和现有 4,800 像素缓冲下连续 50 局／50 次进入退出通过；预热后退出可用内存稳定为 12,248 字节。已测连续运动帧最大更新 8,272 像素，是宿主重绘面积统计，不是真机帧率或传输耗时。
- 每个测试增量帧逐像素对照整屏重绘；前景射手正确遮住远处的球，扑出终点对应实际手套像素，踢高不会覆盖顶栏。测试只在页面转场时切换方向；旧测试每帧重置旋转，不能证明局部刷新正确性。
- 资源转换 `--check`：PASS，共 264,180 字节 const 像素，不做运行时 PNG 解码或缩放。`build/penalty-art-preview/` 内 64 张实际 LVGL 帧覆盖菜单、各结果／方向、难度边缘和每 50 ms 一帧的起脚序列；已目视检查原尺寸合成图。这些是桌面渲染，不是实机截图。
- ASan 因前述宿主运行库启动故障仍为 NOT RUN。实机刷新、可读性、声音／触球时机、电量／供电与耐久尚未验证。

```text
File: build/Penalty-pixel-art-v0.5-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 1186272 bytes
Flash offset: 0x0
SHA-256: 87b51e76dc9e0afc04091805cda6dd13e95ef8bd55d60c22121dae30bcaba3ac
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 实物可读性、刷新、按键响应、声音／触球时机、电量、
  板卡堆／栈／耐久及宿主 ASan
```

新美术验收请使用该版本文件。除下方清单外，重点检查球先接触鞋尖再飞出、左右扑救接到手套、人物移动无残影、绿区／细线仍清晰，长按 OK 恢复原封面。视觉前摇占原 600 ms 中的 100 ms，踢球声音仍在提交时启动。v0.4 与 v0.3 带版本号的旧固件均保持不变。

## v0.4 难度更新（历史本地固件）

已实现三档难度、每球随机绿区、两格必进线、普通绿区概率判定和绿区五分之一速度。游戏内保留几何像素绘制，不接入之前拆分的新素材。本次未刷写设备，未上传或提交社区审核。

- 完整 `./tools/validate.sh`：PASS，使用 ESP-IDF 5.5.3；静态／宿主检查、应用容量、分区布局及合并镜像校验全部通过。记录：`build/penalty-difficulty-validation.log`。
- 纯 C 模型及 UndefinedBehaviorSanitizer：PASS。覆盖全部 101 个力度、九种方向组合与十个随机值，三档宽度的所有绿区起点／内部细线位置，80 ms 必进窗口，升降双向，30,000 次区域生成，输入恢复不重抽，重复／过期事件、五球计分及 100,000 步事件序列。
- 实际 LVGL 9.5.0 渲染：24 KB 池内连续 50 局／50 次进入退出通过，预热后退出可用内存稳定为 12,248 字节。测试检查菜单／结果文字宽度、区域两端的绿区／深绿线逐像素对应、所有游标值，以及电量文字外不变的竖屏封面像素。这是宿主证据，不代表真机内存或时间测量。
- `build/penalty-difficulty-preview/` 内有二十八张 PPM，菜单、瞄准、蓄力、困难档边缘位置和玩法已通过 `review.png` 目视检查。这些是实际桌面 LVGL 渲染，不是实物照片或实机截图。
- 未重跑 AddressSanitizer，之前的宿主运行库启动问题尚未解决；UndefinedBehaviorSanitizer 不能代替 ASan。

```text
File: build/Penalty-difficulty-v0.4-full.bin
Canonical copy: build/FoloToy-AI-Passport-full.bin
Size: 920928 bytes
Flash offset: 0x0
SHA-256: 2751391fa6a36790718479006bb1c3e37ad3218fd86142b9af14c9eb5a0ff92a
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 80 ms 必进线时机、实物可读性／按键响应、声音、电量、
  板卡堆／栈／耐久及宿主 ASan
```

v0.4 真机验收请使用上方带版本号的文件。旧 v0.3 副本保持不变；下方历史记录不代表当前通用文件。

## 里程碑状态

| 里程碑 | 状态 |
| --- | --- |
| M0 — 策划 | 完成，已纳入横屏方案 |
| M1 — 纯 C 规则 | 已实现，宿主测试通过 |
| M2 — 输入与页面闭环 | 已实现，宿主渲染通过；真机按键／旋转待验收 |
| M3 — 像素与声音 | 固件已实现，主流程截图已检查；扬声器听感待验证 |
| M4 — 验收 | 固件构建与合并镜像门禁通过；待用户真机验收及实物测量 |

## v0.3 自动验证证据（2026-09-15，历史记录）

- 仓库静态门禁：PASS，包含文档链接／语言、工作流检查、基线测试、点球模型、导航回归和独立游戏生命周期契约检查。
- 模型按 `-Wall -Wextra -Werror` 编译，本次 UndefinedBehaviorSanitizer 检查通过。本次 AddressSanitizer 未进入测试 `main`：进程采样显示宿主 ASan 运行库在影子内存初始化期间递归锁等待，已停止两个卡住的进程。此前 MVP 的 ASan 通过不代表本修订通过，需在兼容的宿主运行库下补测。
- 规则覆盖：全部九种方向组合及力度边界、蓄力上升／下降、4,000 ms 截止、延迟帧、过期／重复事件、恰好五次、5/5 与 0/5 局、静音、重玩、标题、退出、可复现种子。
- 导航回归：CLICK／DOUBLE／LONG 尾事件保留原 PRESS 所属页面代次，避免长按 OK 返回封面后，松开产生的尾事件又进入横屏。
- 实际 LVGL 9.5.0 渲染：在 24 KB LVGL 池及单个 4,800 像素 RGB565 绘制缓冲下，连续 50 局模拟及 50 次进入／退出通过。桌面构建预热后，退出时 LVGL 可用内存稳定为 12,248 字节。桌面分配大小与耗时不是 ESP32-C3 实测数据。
- 对四分之一圈旋转后的每个像素检查竖横屏圆角分类一致性。文字宽度检查覆盖方向提示、标题、瞄准、蓄力、飞行、六种结果、静音及总结。共生成十七张 PPM 截图（含玩法和返回封面）至 `build/penalty-cover-preview/`，封面和玩法转换为 PNG 后已目视检查。封面在实时电量区域以外逐像素对照内嵌 RGB565 资源及既有圆角遮罩，并检查未知电量／100% 的文字不被裁切。
- 完整 `./tools/validate.sh` 门禁：PASS。环境为 macOS arm64、ESP-IDF v5.5.3、GCC 14.2.0，组件锁未改变。引导程序、分区表 MD5／布局、应用容量及合并字节均已校验。当前镜像大小及校验值见下方记录。这是构建证据，不是真机测试。
- `./tools/preview-penalty.sh` 的 CMake 封装也已通过，使用完整且与锁定组件哈希一致的 LVGL 9.5.0 包。

2026-09-15 构建的验收文件：

```text
File: build/FoloToy-AI-Passport-full.bin
Acceptance copy: build/Penalty-portrait-cover-v0.3-full.bin
Size: 919968 bytes
Flash offset: 0x0
SHA-256: a585b0be9389e8a9425f758580b0040804210a6ee1911b23b0229889c70ad7ab
Build: PASS
Host tests: PASS
Device tests: NOT RUN
Unverified: 下方真机验收清单；本修订宿主 ASan 检查
```

本地构建记录为 `build/penalty-cover-validation.log`。固件和日志均为 Git 忽略的生成产物，重新构建可能改变校验值；请核对实际刷入的文件。

v0.3 社区提交前用同一份源码重新构建，并再次通过完整门禁。提交时通用文件与 `build/Penalty-community-v0.3-full.bin` 一致，SHA-256 为 `26ca8313be811a0167fbec29b3881e94dfce50b27f03a749d63d017f9a7eaf60`，大小仍为 919,968 字节。两个 v0.3 副本保持不变。精确提交文件与历史回执见[社区提交记录](community-submission.zh_CN.md)。本次本地 v0.4 更新未重新查询审核状态。

在仓库根目录复现：

```bash
./tools/validate.sh --static
./tools/preview-penalty.sh /path/to/lvgl-9.5.0
# 激活 ESP-IDF 5.5.3 后：
./tools/validate.sh
```

## 影响验收的实现细节

- 游戏三键均使用 PRESS，忽略随后 CLICK／DOUBLE。物理 UP 对应逻辑右，DOWN 对应左，开机直达竖屏封面，不显示硬件演示菜单。
- 锁定力度使用按键产生时的时间戳；超过 100 ms 的迟到事件使尚未提交的瞄准／蓄力重新同步，不产生延后射门。超时反馈最多额外等待 100 ms，以处理已经入队的按键；有效输入窗口仍按事件时间严格限定为 4,000 ms。
- 难度和静音保留到返回竖屏封面，重玩清零成绩但保留这两项设置，默认难度为简单。只有语言写入现有 NVS 命名空间／键，并在首个封面前恢复；存储缺失或值无效时回退英文。不联网、不新增分区。
- 每个新球只生成一次绿区、内部两格深绿线、门将六格目标和 0–99 结果随机值；输入恢复保留四者。瞄准和蓄力显示相同区域及六格，游标不遮盖细线，绿区双向减至原速五分之一。
- 音频使用四项语义队列、10 ms PCM 分片和协作停止。格式、明确的 60% 音量及写入失败分别可观察，并退化为静音。电量在该 worker 内轮询，不阻塞 LVGL；worker 缺失时仍可游玩，横屏页面显示未知电量且无声音，竖屏封面始终不显示电量。
- 封面／标题／玩法／瞄准／总结空闲 60 秒后，背光降为 15%；第一次 PRESS 仅唤醒，不射门。退出恢复 100% 背光。
- 旋转出错时尝试回滚，保留提示供重试。worker 停止或方向恢复失败时保留页面，长按 OK 重试清理。实物故障注入仍待执行。

## 安装验收固件

交付文件为 `build/FoloToy-AI-Passport-full.bin`，仅在固件门禁通过后生成。它是面向 8 MB Flash 的 ESP32-C3 合并镜像，包含引导程序、分区表和应用程序。使用可传输数据的 USB 线，在 **`0x0` 地址**刷入；不能通过普通文件拷贝完成安装。屏幕初始化后直接显示竖屏 Penalty 封面，再初始化可选音频／电量。

**数据提醒：**合并镜像会替换现有固件，并可能重置 NVS 配置。刷写前备份需要保留的固件／配置，不要勾选整片擦除。如必须保留设备现有数据，应先停止并安排合适的分段更新方案。

本次 ESP-IDF 环境使用 esptool 4.12.0。关闭其他串口监视器，将 `<port>` 替换为实际设备端口，然后在仓库根目录执行：

```bash
python -m esptool --chip esp32c3 -p <port> -b 460800 \
    write_flash 0x0 build/FoloToy-AI-Passport-full.bin
```

`<port>` 是占位符，不能原样输入（macOS 端口通常以 `/dev/cu.usbmodem` 开头，Windows 使用 `COM` 端口）。使用图形化 ESP32 刷写工具时，选择相同芯片、文件和地址。不要把 app-only 镜像写到 `0x0`。这是本文档记录的真机验证所使用的命令。

第一轮先快速检查：方向正确、左右映射、一局五球、声音／静音、长按 OK 退出恢复竖屏。如有问题，请提供出错步骤、实际与预期的差异，以及短视频或脱敏串口日志，并说明能否稳定复现。

## 真机验收清单

空白设备使用经验证的合并镜像；需要保留现有 NVS 时，依照仓库[构建说明](../../docs/development/engineering/build-and-test.zh_CN.md)选择分段烧录。v1.1 镜像已通过这份清单的真机验证。

1. 刷入合并镜像后开机，首个应用画面应为已确认的竖屏像素封面并显示英文横转提示，不再显示 FoloToy 或 Display／Button 菜单，也不显示任何电量文字。设备顺时针横握（原顶部向右）后按一次 OK，标题应正向显示、铺满 320 × 240 逻辑画面、四角黑色圆角正确，并在顶栏显示实时电量。
2. 打开“设置”，循环简单／普通／困难，切换音效，再将语言改为简体中文。确认当前页面立即切换，四行均完整显示；返回标题后打开“说明”，检查中文无缺字、清晰且不裁切。回到设置选定难度，再开始游戏。确认绿区宽度为 20／11／6 格，内部深绿线为两格。从初始中下格开始，连续按物理 UP，确认中下→左下→左上→中上→右上→右下→中下；物理 DOWN 反向环绕。OK 开始蓄力，另一次 OK 锁定。进入游戏的动作不得同时启动射门。
3. 确认六格边框和亮色选中框均清晰可读；蓄力前可见区域，蓄力中固定，下一球换位置。双向经过绿区都减速，检查深绿线的实际可读性与按键时机。体验弱射、深绿线进球／扑出、普通绿区进球／扑出、绿区外进球／扑出、过高和超时；球落点、门将格、结果文案、音效类别须一致，每球恰好记录一次。90%／65%／25% 的精确平均值已有自动模型证据，少量真机随机结果不能重新统计验证。
   简单模式每球保留本地化后的深绿线教学；普通／困难仅第一球显示，之后的本地化鼓励文案在瞄准／蓄力期间保持不变，相邻球不重复，第五球使用专属收官文案。中英文结果解说均须与实际判定一致且不得裁切。
4. 完成五球、重玩、返回标题、切换音效、静音重玩；重玩／标题保留难度和静音，返回封面重置两者。语言应在封面、再次进入游戏及正常断电重启后保持中文；切回英文后重复一次重启检查。不得出现虚构的对手比分或加罚。
5. 从横屏菜单打开“说明”，按 OK 返回菜单。在封面、标题、设置、说明、瞄准、蓄力、飞行、结果、总结分别长按 OK，声音应停止并恢复竖屏封面。松开 OK 不得自动重进横屏菜单。立即重进，确认旧按键／旧音效不会进入新一局。
6. 在封面／标题／玩法／瞄准／总结等待 60 秒，检查变暗和仅唤醒一次按键、不误射门；确认封面始终无电量文字，横屏页面的电量／未知电量文字完整且更新不影响游玩。
7. 连续游玩 20 局并执行 50 次进入／退出，查看 `penalty` 日志中的进入／退出堆、最大连续块、最小堆、worker 栈余量、`max_update_us`、`max_input_age_ms`。比较预热后的数据，不应持续丢失堆、泄漏任务、触发看门狗或重启。
8. 测量输入到可见反馈是否达到 100 ms 目标，动画是否达到暂定 20 FPS。`max_update_us` 仅是模型／UI 更新耗时，不包含最终绘制／SPI 传输；它和宿主截图均不能证明实物帧率。检查实物尺寸下的按键触达、反光、文字可读性和圆角裁剪。
9. 先按固件内置的 60% 音量试听。确认蓄力、踢球、普通进球、精准进球、普通／绿区／深绿线扑救、弱射、过高／超时、难度、静音和五球结束提示可区分；超时不得出现踢球声。用慢动作视频检查踢球声开始与画面 100 ms 触球点相差约 50 ms 以内。检查削波／杂音、SOUND 关闭前最后提示、重新开启提示、快速退出重进取消及连续 20 局。只有确有需要再索取 40%／80% 校准固件。在受控开发环境中测试音频／电量不可用，以及 worker／旋转失败，确认可安全恢复且长按 OK 有效。

待验证：实际旋转方向、中文实物可读性与语言持久保存、输入延迟、显示帧率、音频质量、电量读数、系统堆／任务栈、故障注入和真机耐久循环。
